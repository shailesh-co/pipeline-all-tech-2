pipeline {
    agent any

    environment {
        FRONT_IMAGE = 'shailesh01/ui-frontend:latest'
        BACK_IMAGE  = 'shailesh01/node-backend:latest'
    }

    options {
        timestamps()
    }

    stages {
        stage('Clone Git Repository') {
            steps {
                wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'xterm']) {
                    git branch: 'main', url: 'https://github.com/shailesh-co/pipeline-all-tech-2.git'
                }
            }
        }

        stage('Build Frontend Docker Image') {
            steps {
                wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'xterm']) {
                    dir('UI-FrontEnd') {
                        script {
                            sh "docker build -t ${env.FRONT_IMAGE} -f ../docker/frontend.Dockerfile ."
                        }
                    }
                }
            }
        }

        stage('Build Backend Docker Image') {
            steps {
                wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'xterm']) {
                    dir('node-backend') {
                        script {
                            sh "docker build -t ${env.BACK_IMAGE} -f ../docker/backend.Dockerfile ."
                        }
                    }
                }
            }
        }

        stage('Push Docker Images to Docker Hub') {
            steps {
                wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'xterm']) {
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                        sh '''
                            echo "$PASS" | docker login -u "$USER" --password-stdin
                            docker push $FRONT_IMAGE
                            docker push $BACK_IMAGE
                            docker logout
                        '''
                    }
                }
            }
        }

        stage('Deploy to Server via Ansible') {
            steps {
                wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'xterm']) {
                    sh 'ansible-playbook ansible/deploy.yaml -i inventory/hosts'
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed.'
            cleanWs()
        }
        failure {
            echo 'Pipeline failed. Please check the error logs above.'
        }
    }
}
